spring.application.name=HanaZoom

# Active Spring Profile
# 기본적으로 dev 프로파일을 활성화하여 application-dev.properties를 로드합니다.
spring.profiles.active=dev

# Database Configuration (MySQL for Docker Compose)
spring.datasource.url=jdbc:mysql://localhost:3307/hanazoom?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=hanazoom_user
spring.datasource.password=hanazoom1234!

# Redis Configuration (Docker Compose Redis)
spring.data.redis.host=localhost
spring.data.redis.port=6380
spring.data.redis.password=redis1234!
spring.data.redis.timeout=2000ms

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# DevTools Configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# Logging Configuration
logging.file.name=/var/log/hanazoom/backend.log
logging.level.root=INFO
logging.level.com.hanazoom=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Server Configuration
server.port=8080

# Kakao API Configuration
# 실제 키 값은 application-dev.properties 또는 OS 환경 변수에서 주입됩니다.
kakao.rest.api.key=${KAKAO_REST_API_KEY:}